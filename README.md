# demo_planning_msgs

This ROS service definition is intended to be used with the 'unity' branch of:

https://github.com/s-marios/plan_example_python

Place this service definition in the same ROS workspace and compile using:

```
$ colcon build
```
